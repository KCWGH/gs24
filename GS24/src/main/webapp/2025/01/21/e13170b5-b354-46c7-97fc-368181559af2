$ cd desktop/gsproject 깃허브와 연결된 폴더 열기

$ git add .

$ git stash 임시저장

$ git pull origin main 파일 받아오기

$ git stash apply 임시저장한 파일 적용하기

$ git status 파일 확인

$ git add .  

$ git commit -m "대충 잘 함" 커밋하면서 메시지 보내기

$ git push origin main 깃허브에 파일 올리기
